import React from 'react'

const About = () => {
    return (
        <div>
            <h1>About Ledger</h1>
            <p>Ledger is a personal finance tracker built on one simple principle: your balance should always be provable, not just stored. Every deposit, withdrawal, and loan is recorded as a transaction — your current balance is calculated from that history, not kept as a separate number that can drift out of sync.
            We built Ledger to give people a clear, auditable view of their own money — the same way a bank's internal ledger works, but visible to you.</p>
        </div>
    )
}

export default About
