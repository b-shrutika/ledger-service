import React from 'react'

const Hero = () => {
  return (
    <div>
        <h1>Ledger that keeps every transaction accounted for.</h1>
        <p>Track deposits, withdrawals, and loans in one transparent ledger - your balance, always derived, never guessed.</p>

        <button>Get Started</button>
        <button>Login</button>
    </div>
  )
}

export default Hero
